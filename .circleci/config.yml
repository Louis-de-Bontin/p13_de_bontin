version: 2.1
orbs:
  docker: circleci/docker@2.0.2
jobs:
  example-job:
    docker:
      # replace with your preferred image
      - image: cimg/base:stable
    steps:
      - docker/build:
          image: oc-lettings 
workflows:
  example-workflow:
    jobs:
      - example-job